def xpto(a: int, b: int, c: str) -> list[int]:
    """This was generated

    Args:
        a (int): Pois é `int`
        b (int): né **bold**
        c (str): hello

    Returns:
        list[int]: allo, tas ocupada?
    """
    x = b
    y = a
    return [a, b]

l = xpto(1,2,'carlos')